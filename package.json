{
  "private": true,
  "name": "varejo360",
  "version": "1.0.0",
  "workspaces": [
    "server",
    "catalogo-leite"
  ],
  "scripts": {
    "dev:web": "yarn workspace catalogo-leite start",
    "dev:server": "yarn workspace server ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/server.ts "
  }
}
