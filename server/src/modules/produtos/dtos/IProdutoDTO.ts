export default interface IProdutoDTO {
  codigo: string;
  nome: string;
}

/**
 *  (DTO) ou simplesmente Transfer Object é um padrão de projetos para
 * transporte de dados entre diferentes componentes de um sistema,
 * diferentes instâncias ou processos
 */
